interface:
  display_name: "GitHub PR Context"
  short_description: "Collect GitHub PR metadata, files, commits, and comments"
  default_prompt: "Use $github-pr-context to fetch the normalized GitHub context bundle for this PR URL and return the JSON evidence."
